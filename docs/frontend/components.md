# Frontend components

| Component | Responsibility | Status |
| --- | --- | --- |
| `AskKisanApp` | Owns query state, anonymous session continuity, and API interaction. | Implemented |
| `Header` | Provides minimal product identity. | Implemented |
| `SearchBar` | Accessible question entry and submission. | Implemented |
| `ResultPanel` | Renders response, source IDs, soft loading skeleton, and error state. | Implemented |

Proposed MVP components include a question input, optional context selector, answer card, source/reference display, clarification prompt, and clear error/empty states. These are product targets, not an approved UI implementation.
