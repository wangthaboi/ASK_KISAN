# Data model

No database data model is verified yet. The embedding script expects a table named `vector_embeddings` with `id`, `content`, and `vector` fields, but this is an application expectation rather than confirmed schema.

Agriculture, schemes, districts, advisory categories, user/private-data, and reference entities are all **Uncertain** until metadata inspection is available. Do not create a speculative ERD here.
